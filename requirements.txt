# ── Runtime dependencies (required to RUN the app) ──
# These are all you need on a fresh system. No XGBoost needed!
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
onnxruntime>=1.16.0
pandas>=2.0.0
numpy>=1.24.0
httpx>=0.25.0
